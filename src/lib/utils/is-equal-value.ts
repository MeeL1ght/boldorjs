/**
 * Compare two values and determine if they are strictly the same.
 * @param {any} firstValue
 * @param {any} secondValue
 * @returns {boolean} `boolean`
 */
const isEqualValue = (firstValue: any, secondValue: any): boolean =>
	firstValue === secondValue ? true : false

export default isEqualValue
